#include <stdio.h>

/**
 * main - the start point of execution program
 * Return: always 0 (successfull)
 */

int main(void)
{
char i;
for (i = 'a' ; i <= 'z'; i++)
{
putchar(i);
}
putchar('\n');
return (0);
}
