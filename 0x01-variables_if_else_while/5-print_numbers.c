#include <stdio.h>

/**
 * main - start point of execution program
 * Return: always 0 (successfull)
 */

int main(void)
{
int i;
for (i = 0; i < 10; i++)
{
printf("%d", i);
}
printf("\n");
return (0);
}
